class CartItem {
    constructor(quantity, productTitle,pushToken, productPrice ,sum){
        this.quantity = quantity
        this.productTitle = productTitle,
        this.pushToken = pushToken,
        this.productPrice = productPrice,
        this.sum = sum

    }
}

export default CartItem